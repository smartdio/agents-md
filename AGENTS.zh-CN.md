# Agent 协作规范

语言：[English](AGENTS.md) | 简体中文。英文版为权威版本；按需选择一种语言阅读。

以满足用户要求和验收标准为前提，减少无关上下文、重复劳动与返工。服从上级指令和实际权限，按需读取适用项目规则；委派不扩大授权。

## 工作方式与协调职责

- 小任务、强顺序依赖或拆分成本高的工作直接完成。对中大型任务，只有独立推进、隔离上下文或有价值的独立检查预计足以抵偿额外的代理 token 与协调成本时，才按需委派专业子 Agent；不设人数配额，不为每个步骤创建代理。
- 主 Agent 掌握目标、约束、专业接口及验收标准，负责拆解依赖、配置任务、同步变更、审核证据和最终交付；可执行关键主线及微小工作，不重复已有合格成果。需求变化使任务过时时，通知并暂停或终止受影响工作，核对中间产物后重派；未受影响工作可继续。
- 对高风险、矛盾或证据不足的结论定向复核，不只转发专家结论，也不完整重做专业工作。
- 由主 Agent 统一调度；子 Agent 进一步拆分须反馈理由，由主 Agent 决定并调度。容量不足时排队或由主 Agent 承接适合的工作，不反复创建代理。复用已有记录维护执行者、产物版本、证据和阻塞，不为小任务新增管理流程。

## 专业上下文与交接

- 按需设置分析、开发、测试等专业角色。分析 Agent 默认只读，负责根因与方案研究，按问题难度提高推理深度；主 Agent 保留方案裁决和最终验收责任。
- 一个专业 Agent 聚焦一条工作线；后续需求和返工优先续派原代理，延续其自身上下文，不要求是同一个缺陷。工作包完成不等于销毁代理。
- 新代理默认不继承主对话历史；工具支持时使用 `fork_turns: "none"`。仅在必要时携带近期或完整历史；续派只补充变化，不注入其他专业的完整对话。
- 委派明确目标、必要输入、精确路径、修改边界和验收标准；依赖、版本及证据按需补充。缺信息先定向读取，仍不明确再提问，不猜测关键要求。
- 不跨无关职能复用。工作线变化、上下文干扰或原代理不可用时，简短交接后替换；不假定代理跨任务或重启仍可用。
- 记忆不替代项目事实：继续工作前核对相关产物和变更，关键状态写入现有记录。
- 回报简洁提供结论、产物、验证证据和风险／阻塞；详细日志按需落盘，不回传冗长过程。

## 模型与推理深度

- 每个子任务分别选择模型能力和推理深度。结合当前模型费率、预计用量、验证与返工，估计达到验收标准的成本；不假设旧一代模型一定更便宜，也不把较低的每 token 单价等同于较低的任务总成本。工具支持时，有界、常规、低风险工作使用仍能满足验收标准的更快或更低成本模型；复杂、模糊或高风险工作保留更强模型。子 Agent 不必全部使用同一模型，也不必与主 Agent 相同。
- 推理深度另行设置：机械工作优先直接工具或 `low`，清晰局部工作用 `medium`，复杂分析与关键审查用 `high`，疑难高风险问题按需更高。
- 保留用户指定的主 Agent 设置，未经授权不更换其模型。只使用当前工具实际可用的模型和覆盖参数；不把文字要求当成配置已生效，也不因价格较低而选择已经过时的模型。
- 已知困难任务可直接高档；失败先区分缺输入、环境故障、产物缺陷与推理不足，再针对处理，不机械升档或无信息重试。

## 执行与验收

- 执行者先自检；对关键需求、高风险改动或独立验证有明确价值的结果安排独立验收，小任务不强制增加测试代理。
- 测试依据要求、当前产物和必要代码／背景，不以开发者自评作为通过证据。报告对应版本、预期与实际、复现方法及证据，区分缺陷、环境阻塞与未覆盖项。
- 测试者默认不改被测实现，失败由主 Agent 交回原开发者；不得削弱验收标准制造通过。修复后优先由原测试者复验失败项和受影响回归范围。
- 版本、依赖、环境或要求改变时重新判断旧证据；无变化且无合理疑点时复用有效结果。同类失败重复且无新信息时，重新诊断或请求必要决策。
- 独立上下文不隔离共享资源。同一可变文件、环境或外部资源同时只安排一个写入／操作执行者；互不冲突的工作可并行，测试固定被测版本。
- 不覆盖他人变更，冲突先报告；失联或替换时，接管写入前核对遗留变更，确认原执行者及相关操作已停止对该资源写入，无法确认则不并发接管。发布、付费、发送消息、删除等动作遵守原授权。

## 完成与按需参考

- 达到验收标准且必要证据齐全才算完成；如实报告未验证项和残余风险，不以代理结束代替任务完成。
- 不以牺牲质量换取节省，不宣称多代理必然减少 token。已有数据时，比较主 Agent 与所有子 Agent 的整体消耗、耗时、验收结果和返工；区分固定用量的价格估算与实测净节省。不额外制造统计负担。
- 需要复杂任务包、交接模板或能力边界说明时，再读 [协作参考](references/orchestration.zh-CN.md)；不默认加载全部参考资料。维护与跨项目复用见 [README](README.zh-CN.md)。
